fn main() {
    /*
    let s = "1234567890".repeat(10) + "Error" + "abcde".repeat(10).as_str();
    let v = vec![
        s.clone(),
        s.clone(),
        s.clone(),
        s.clone(),
        s.clone(),
        s.clone(),
        s.clone(),
        s.clone(),
        s.clone(),
        s.clone(),
    ];
    let vv: Vec<&str> = v.iter().map(|item| item.as_str()).collect();
    //
    let n = cmp_pattern_match::do_match_string(&vv, "Error");
    assert_eq!(n.unwrap(), 10);
    */
}
